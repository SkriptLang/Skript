name: Java 21 CI (MC 1.20.6+)

on:
    push:
        branches:
            - master
            - 'dev/**'
    pull_request:

jobs:
    build:
        if: "! contains(toJSON(github.event.commits.*.message), '[ci skip]')"
        runs-on: ubuntu-latest
        strategy:
            matrix:
                minecraft-version: [1.20.6, 1.21.3, 1.21.4, 1.21.5, 1.21.8, 1.21.10]
        steps:
            - uses: actions/checkout@v5
              with:
                  submodules: recursive
            - name: validate gradle wrapper
              uses: gradle/actions/wrapper-validation@v4
            - name: Set up JDK 21
              uses: actions/setup-java@v5
              with:
                  java-version: '21'
                  distribution: 'adopt'
                  cache: gradle
            - name: Grant execute permission for gradlew
              run: chmod +x gradlew
            - name: Build Skript and run test scripts
              run: ./gradlew clean specificTest -PtestEnv=java21/${{ matrix.minecraft-version }} -PtestEnvJavaVersion=21
            - name: Upload Nightly Build
              uses: actions/upload-artifact@v4
              if: success()
              with:
                  name: skript-nightly
                  path: build/libs/*
