test "ExprLootContextKillerJUnit" when running JUnit:
	set {_tests::1} to "set killer"
	set {_tests::2} to "clear killer"
	ensure junit test "org.skriptlang.skript.test.tests.syntaxes.expressions.ExprLootContextLooterTest" completes {_tests::*}

on join:
	set {_test} to "org.skriptlang.skript.test.tests.syntaxes.expressions.ExprLootContextLooterTest"
	junit test is {_test}

	set {_player} to player
	set {_context} to a loot context at player's location:
		set loot context killer to {_player}

		if loot context killer is {_player}:
			complete objective "set killer" for {_test}

		reset loot context killer

		if loot context killer is not set:
			complete objective "clear killer" for {_test}
